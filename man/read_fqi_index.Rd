% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_fqi_index.R
\name{read_fqi_index}
\alias{read_fqi_index}
\title{Read one or more FastqIndEx \code{.fqi} binary indexes}
\usage{
read_fqi_index(fqi_path, files = NULL, type = c("auto", "fasta", "fastq"))
}
\arguments{
\item{fqi_path}{Character vector of \code{.fqi} file paths.}

\item{files}{Optional character vector of indexed gz file paths. When omitted,
paths are deduced using the convention that \code{.fqi} is appended to the source
filename.}

\item{type}{Record format: \code{"auto"}, \code{"fasta"}, or \code{"fastq"}. For \code{"auto"},
type is inferred from the first non-empty line of each available source
file (\code{>} for FASTA, \code{@} for FASTQ). If detection fails for all files, a
warning is issued and \code{"fastq"} is used.}
}
\value{
A \code{fastqindexr_index} object with subclass
\code{fastqindexr_gzip_index}.
}
\description{
Reads one or more \code{.fqi} index files produced by the FastqIndEx CLI and
converts them into a \code{fastqindexr_index} object that works with
\code{\link[=extract_sequences]{extract_sequences()}}.
}
