% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_sequences.R
\name{extract_sequences_to_file}
\alias{extract_sequences_to_file}
\title{Extract sequence records by ID and stream directly to file}
\usage{
extract_sequences_to_file(
  index = NULL,
  seq_idx,
  file = NULL,
  outfile,
  type = c("auto", "fasta", "fastq"),
  append = FALSE,
  compress = endsWith(tolower(outfile), ".gz"),
  collapse_sequence_lines = FALSE,
  mode = c("auto", "indexed", "sequential"),
  input_type = c("auto", "fasta", "fastq"),
  renumber = c("none", "zero_based", "one_based")
)
}
\arguments{
\item{index}{Either a \code{fastqindexr_index} object (from \code{\link[=create_index]{create_index()}} or
\code{\link[=read_fqi_index]{read_fqi_index()}}) or one or more \code{.fqi} file paths.}

\item{seq_idx}{Numeric vector of record IDs (positive whole numbers). Values
are coerced via \code{\link[=as.numeric]{as.numeric()}}; \code{NA} and values outside \code{1} ... \code{n_records}
are errors.}

\item{file}{Optional character vector of file paths overriding those stored
in \code{index$files} for object input. For \code{.fqi} path input, this provides
indexed gz file path(s) passed to \code{\link[=read_fqi_index]{read_fqi_index()}}. If omitted for \code{.fqi}
input, file paths are deduced from the \code{.fqi} names.}

\item{outfile}{Output file path.}

\item{type}{Output format: \code{"auto"} (default), \code{"fasta"}, or \code{"fastq"}.
\code{"auto"} uses the indexed input format. FASTQ input can be emitted as FASTA;
FASTA input cannot be emitted as FASTQ. When emitting FASTA from a FASTQ
index, quality lines are not read from the source.}

\item{append}{Logical; append to existing \code{outfile} if \code{TRUE}, otherwise
overwrite. When \code{append} is \code{FALSE} and \code{seq_idx} is \strong{strictly
increasing} (no duplicate IDs), extraction writes each record as it is
read and does not build a full in-memory map of all unique records. When
\code{append} is \code{TRUE} or the request is not strictly increasing, the
implementation may buffer all unique records like \code{\link[=extract_sequences]{extract_sequences()}}.}

\item{compress}{Logical; if \code{TRUE}, write gzip-compressed output directly via
zlib. Defaults to \code{endsWith(tolower(outfile), ".gz")}.}

\item{collapse_sequence_lines}{Logical scalar. For FASTA output only: when
\code{FALSE} (default), wrapped sequence lines from input FASTA are preserved in
extracted sequence text; when \code{TRUE}, wrapped lines are concatenated into a
single line per record.}

\item{mode}{\code{"auto"} uses index-backed extraction when \code{index} is non-\code{NULL}
and \code{getOption("fastqindexr.extract_mode", "indexed")} is \code{"indexed"};
otherwise (no index, or option \code{"sequential"}) reads by streaming scans.
\code{"indexed"} requires \code{index} and offset-based I/O. \code{"sequential"} always
streams and does not require a live native index pointer (works with
\code{index} fields after \code{\link[=readRDS]{readRDS()}} if paths remain valid).}

\item{input_type}{When \code{index} is \code{NULL}, passed to format detection:
\code{"auto"}, \code{"fasta"}, or \code{"fastq"}. Ignored when \code{index} is provided.}

\item{renumber}{Renames emitted \code{seq_id} / FASTA headers: \code{"none"},
\code{"zero_based"}, or \code{"one_based"} (output order positions). For
non-\code{"none"} values, extraction is routed through \code{\link[=extract_sequences]{extract_sequences()}}
before writing.}
}
\value{
Invisibly returns \code{outfile}.
}
\description{
Writes records in \strong{the same order as \code{seq_idx}}, including duplicate IDs.
IDs are \strong{1-based} record indices in the logical concatenated stream defined
when the index was built (see \code{\link[=create_index]{create_index()}}).
}
\seealso{
\code{\link[=extract_sequences]{extract_sequences()}}, \code{\link[=create_index]{create_index()}}
}
