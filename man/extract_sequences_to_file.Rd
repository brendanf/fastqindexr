% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_sequences.R
\name{extract_sequences_to_file}
\alias{extract_sequences_to_file}
\title{Extract sequence records by ID and stream directly to file}
\usage{
extract_sequences_to_file(
  index,
  seq_idx,
  file = NULL,
  outfile,
  type = c("auto", "fasta", "fastq"),
  append = FALSE,
  compress = endsWith(tolower(outfile), ".gz")
)
}
\arguments{
\item{index}{Either a \code{fastqindexr_index} object (from \code{\link[=create_index]{create_index()}} or
\code{\link[=read_fqi_index]{read_fqi_index()}}) or one or more \code{.fqi} file paths.}

\item{seq_idx}{Numeric vector of record IDs (positive whole numbers). Values
are coerced via \code{\link[=as.numeric]{as.numeric()}}; \code{NA} and values outside \code{1} ... \code{n_records}
are errors.}

\item{file}{Optional character vector of file paths overriding those stored
in \code{index$files} for object input. For \code{.fqi} path input, this provides
indexed gz file path(s) passed to \code{\link[=read_fqi_index]{read_fqi_index()}}. If omitted for \code{.fqi}
input, file paths are deduced from the \code{.fqi} names.}

\item{outfile}{Output file path.}

\item{type}{Output format: \code{"auto"} (default), \code{"fasta"}, or \code{"fastq"}.
\code{"auto"} uses the indexed input format. FASTQ input can be emitted as FASTA;
FASTA input cannot be emitted as FASTQ.}

\item{append}{Logical; append to existing \code{outfile} if \code{TRUE}, otherwise
overwrite.}

\item{compress}{Logical; if \code{TRUE}, write gzip-compressed output directly via
zlib. Defaults to \code{endsWith(tolower(outfile), ".gz")}.}
}
\value{
Invisibly returns \code{outfile}.
}
\description{
Writes records in \strong{the same order as \code{seq_idx}}, including duplicate IDs.
IDs are \strong{1-based} record indices in the logical concatenated stream defined
when the index was built (see \code{\link[=create_index]{create_index()}}).
}
\section{FASTA limitation}{

Each record must consist of a header line plus \strong{one} sequence line.
}

\seealso{
\code{\link[=extract_sequences]{extract_sequences()}}, \code{\link[=create_index]{create_index()}}
}
