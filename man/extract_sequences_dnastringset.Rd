% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_sequences.R
\name{extract_sequences_dnastringset}
\alias{extract_sequences_dnastringset}
\title{Extract sequence records as a DNAStringSet}
\usage{
extract_sequences_dnastringset(
  index,
  seq_idx,
  file = NULL,
  renumber = c("none", "zero_based", "one_based"),
  chunk_chars = 1e+07
)
}
\arguments{
\item{index}{Either a \code{fastqindexr_index} object (from \code{\link[=create_index]{create_index()}} or
\code{\link[=read_fqi_index]{read_fqi_index()}}) or one or more \code{.fqi} file paths.}

\item{seq_idx}{Numeric vector of record IDs (positive whole numbers).}

\item{file}{Optional character vector of file paths overriding those stored
in \code{index$files} for object input.}

\item{renumber}{Name handling for returned sequences: \code{"none"} keeps source
IDs, \code{"zero_based"} uses \code{"0"}, \code{"1"}, ..., and \code{"one_based"} uses
\code{"1"}, \code{"2"}, ....}

\item{chunk_chars}{Approximate number of sequence characters to process per
chunk when building the return object. Larger values may reduce overhead
but increase temporary memory usage.}
}
\value{
A \code{Biostrings::DNAStringSet}.
}
\description{
Returns sequences as a \code{Biostrings::DNAStringSet} while preserving request
order and duplicate IDs. Extraction is processed in chunks to keep memory
usage bounded on large requests.
}
