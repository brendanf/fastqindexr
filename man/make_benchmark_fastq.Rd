% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_benchmark_fasta.R
\name{make_benchmark_fastq}
\alias{make_benchmark_fastq}
\title{Write a synthetic FASTQ file for benchmarking}
\usage{
make_benchmark_fastq(
  path,
  n = 5000,
  width = 80,
  alphabet = c("A", "C", "G", "T"),
  qual_alphabet = c("!", "#", "$", "\%", "&", "(", ")", "*", "+", "-", ".")
)
}
\arguments{
\item{path}{Output file path. If it ends in \code{.gz} (case-insensitive), output
is gzip-compressed; otherwise, plain text FASTQ is written.}

\item{n}{Number of FASTQ records to write.}

\item{width}{Sequence width (bases) per record.}

\item{alphabet}{Character vector of symbols used to generate sequence lines.}

\item{qual_alphabet}{Character vector of symbols used to generate quality
lines.}
}
\value{
Invisibly returns \code{path}.
}
\description{
Creates a FASTQ file where each record has a deterministic ID
(\code{seq00001}, \code{seq00002}, ...) and randomly sampled DNA sequence/quality
strings of fixed width.
}
\examples{
path <- tempfile(fileext = ".fq")
make_benchmark_fastq(path, n = 3, width = 10)
file.exists(path)
unlink(path)

}
